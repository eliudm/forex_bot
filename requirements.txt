pandas>=2.1.0
numpy>=1.24.0
scikit-learn>=1.3.0
requests>=2.31.0
APScheduler>=3.10.4
tqdm>=4.66.1
python-dotenv>=1.0.0
Flask>=3.0.0

# Only needed for BROKER_MODE=MT5 (live/demo Deriv trading via the MT5 terminal).
# Windows-only, so it's kept optional rather than a hard requirement — install
# it yourself when you're ready to go beyond paper trading:
#   pip install MetaTrader5>=5.0.5488
